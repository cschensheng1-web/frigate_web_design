# web-design 功能说明

跨摄像头多目标跟踪管理平台前端，基于 Frigate 前端框架改造。本文档说明各页面提供的功能。

## 页面总览

| 菜单 | 路由 | 权限 | 功能概要 |
|------|------|------|---------|
| 📹 实时监控 | `/` | 登录用户 | 多路摄像头实时画面 |
| 🔍 智能分析 | `/videopipe` | 登录用户 | VideoPipe 检测结果展示 |
| 🛤️ 人员轨迹 | `/trajectories` | 登录用户 | 人员跨摄像头轨迹查询 |
| 📐 内部标定 | `/calibration` | 管理员 | 单/双/三摄像头标定 |
| 🌍 场地坐标标定 | `/calibration/world` | 管理员 | 世界坐标 → 图像外参标定 |

---

## 一、实时监控（`/`）

显示摄像头的实时画面。

**功能**：
- 多路摄像头网格布局，每路独立显示画面
- 摄像头由 Frigate 配置提供（`config.cameras`）
- 画面由 Frigate 将摄像头 RTSP 转为浏览器可播流
- 支持全屏（快捷键 `f`）
- 点击摄像头进入单路放大视图

**依赖**：Frigate 后端（`localhost:5000`）

---

## 二、智能分析（`/videopipe`）

展示 VideoPipe 视频管道的检测结果。

**功能**：
- **HLS 视频流**：播放 VideoPipe 处理后的视频（`processed_stream_url`）
- **检测框**：画面上叠加目标检测框
- **目标信息**：每个检测目标的
  - `track_id`：跟踪编号
  - `global_person_id`：全局人员 ID（P-XXXX）
  - `label`：目标类别
  - `confidence`：置信度
  - `bbox`：检测框坐标
  - `world_x` / `world_y`：世界坐标
- **状态指标**：
  - 运行状态（在线/等待联调）
  - 推理帧率（FPS）
  - 视频管道数量
  - 当前目标数
  - 运行时长
- **摄像头切换**：下拉选择查看不同摄像头

**接口**：`GET /api/videopipe/overview`

**说明**：接口未连接时显示演示数据，顶部标注"接口待连接"。

---

## 三、人员轨迹（`/trajectories`）

查询人员跨摄像头的移动轨迹。

**筛选条件**：
- 时间范围（起止时间）
- 人员（全局人员 ID）
- 摄像头

**展示内容**：
- **轨迹列表**：符合条件的人员轨迹
- **场地坐标图**：轨迹点在地面坐标系上的可视化
- **跨摄像头分段**：人员经过的摄像头序列（不同摄像头不同颜色）
- **轨迹明细**：每段轨迹的坐标点、时间、置信度
- **统计指标**：
  - 轨迹数量
  - 覆盖摄像头数量
  - 当前轨迹时长
  - 平均置信度

**接口**：`GET /api/person-trajectories`

**说明**：接口未连接时显示演示数据。

---

## 四、内部标定（`/calibration`）

摄像头内参 / 外参标定工作台。

### 标定模式

| 模式 | 说明 |
|------|------|
| **单摄像头** | 一台摄像头采集标定板图像 → 计算内参 |
| **双摄像头** | 两台摄像头同步采集 → 计算相对外参 |
| **三摄像头** | 三台同步采集 → 三组配对联合标定 |

### 功能

- **摄像头管理**：添加 / 编辑 / 删除摄像头（名称、IP、编码、分辨率、RTSP）
- **摄像头选择**：主摄像头 / 副摄像头 / 第三摄像头
- **标定板参数**：`squaresX`、`squaresY`、`squareLength`、`markerLength`、`dictionary`
- **采集**：
  - 单摄「采集一张」
  - 双摄「同步采集一组」
  - 三摄「同步采集三路」
- **图片导入**：从本地导入标定图片
- **采集管理**：单张删除、重置
- **执行标定**：「开始标定」/「开始联合标定」/「开始三摄标定」
- **结果展示**：
  - 重投影误差
  - 内参矩阵（cameraMatrix）
  - 畸变系数（distCoeffs）
  - 外参 / 平移向量
  - 配对数量（联合标定）
- **保存结果**：保存修改到后端

**接口**：
- `POST /api/calibration/run` 单摄像头标定
- `POST /api/calibration/stereo/run` 双/三摄像头联合标定
- `POST /api/calibration/:camera/save` 保存结果

**说明**：仅管理员可访问。依赖标定后端（`192.168.0.102:5001`）。

---

## 五、场地坐标标定（`/calibration/world`）

建立世界坐标系与摄像头图像的映射关系，计算外参。

### 功能

1. **世界坐标点导入**
   - 导入 CSV 文件（格式：`point_id, x, y, z`）
   - 显示坐标点列表（编号、坐标 m）
   - 有效点数统计

2. **画面获取**
   - 抓取高清帧
   - 导入现场图片

3. **像素标注**
   - 在画面上点击标注世界点在图像中的像素位置
   - 撤销最后一个点
   - 显示当前摄像头位置

4. **外参计算**
   - 每台摄像头至少 6 个点
   - 计算重投影误差
   - 查看外参结果（T_world_camera）

5. **保存**
   - 保存坐标点
   - 保存像素标注
   - 保存当前外参

**接口**：
- `PUT wildtrack/world-points` 保存世界坐标点
- `POST wildtrack/calibrate` 计算外参
- `POST wildtrack/:cameraId/save` 保存外参

**说明**：仅管理员可访问。依赖标定后端。

---

## 六、通用特性

- **深色 / 浅色主题**：跟随系统或手动切换
- **多语言**：中英文（i18next）
- **权限控制**：
  - 实时监控 / 智能分析 / 人员轨迹：登录用户
  - 内部标定 / 场地坐标标定：仅管理员
- **演示数据**：接口未连接时显示带"待连接"标识的演示数据，接通后自动切换真实数据
